import { Router } from 'express';
import * as controller from './reconciliation.controller';

const router = Router();

// TODO: define reconciliation routes
// router.get('/', controller.list);

export default router;
