# Public base URL the browser uses for API calls.
# Leave as /api to use the Next.js dev rewrite proxy (same-origin, no CORS).
NEXT_PUBLIC_API_URL=/api

# Backend origin the Next.js rewrite proxies to (server-side, dev only).
BACKEND_URL=http://localhost:3001
